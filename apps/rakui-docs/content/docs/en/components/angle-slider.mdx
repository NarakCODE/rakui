---
title: Angle Slider
description: An interactive circular slider for selecting angles with support for single values and ranges.
---

An interactive circular slider for selecting angles with support for single values and ranges.

<ComponentTabs name="angle-slider-demo" />

## Installation

<CodeTabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">
      Command
    </TabsTrigger>

    <TabsTrigger value="manual">
      Manual
    </TabsTrigger>
  </TabsList>

  <TabsContent value="cli">
    <CodeBlockTabs defaultValue="npm" groupId="package-manager">
      <CodeBlockTabsList>
        <CodeBlockTabsTrigger value="npm">npm</CodeBlockTabsTrigger>
        <CodeBlockTabsTrigger value="pnpm">pnpm</CodeBlockTabsTrigger>
        <CodeBlockTabsTrigger value="yarn">yarn</CodeBlockTabsTrigger>
        <CodeBlockTabsTrigger value="bun">bun</CodeBlockTabsTrigger>
      </CodeBlockTabsList>

      <CodeBlockTab value="npm">
        ```bash
        npx shadcn@latest add @rakui/angle-slider
        ```
      </CodeBlockTab>

      <CodeBlockTab value="pnpm">
        ```bash
        pnpm dlx shadcn@latest add @rakui/angle-slider
        ```
      </CodeBlockTab>

      <CodeBlockTab value="yarn">
        ```bash
        yarn dlx shadcn@latest add @rakui/angle-slider
        ```
      </CodeBlockTab>

      <CodeBlockTab value="bun">
        ```bash
        bun x shadcn@latest add @rakui/angle-slider
        ```
      </CodeBlockTab>
    </CodeBlockTabs>
  </TabsContent>

  <TabsContent value="manual">
    <Steps className="pt-2">
      <Step>
        Copy and paste the following code into your project.
      </Step>

      <ComponentSource name="angle-slider" />

      <Step>
        Update the import paths to match your project setup.
      </Step>
    </Steps>
  </TabsContent>
</CodeTabs>
## Layout

---

Import the parts and compose them together.

```tsx title="angle-slider-snippet.tsx"
import {
  AngleSlider,
  AngleSliderRange,
  AngleSliderThumb,
  AngleSliderTrack,
  AngleSliderValue,
} from "@rakui/angle-slider";

export default function Example() {
  return (
    <AngleSlider>
      <AngleSliderTrack>
        <AngleSliderRange />
      </AngleSliderTrack>
      <AngleSliderThumb />
      <AngleSliderValue />
    </AngleSlider>
  );
}
```

## Examples

### Controlled State

A slider with controlled state management and custom actions.

<ComponentTabs name="angle-slider-controlled-demo" />

### Range Selection

Use multiple thumbs to create angle ranges with minimum step constraints.

<ComponentTabs name="angle-slider-range-demo" />

### Themes

Slider variants with different themes.

<ComponentTabs name="angle-slider-themes-demo" />

### With Form

Integrate the angle slider with form validation and submission.

<ComponentTabs name="angle-slider-form-demo" />

## Theming

### Track Theming

Use `[&>[data-slot='angle-slider-track-rail']]` to style the background track:

```tsx title="angle-slider-snippet.tsx"
<AngleSliderTrack className="*:data-[slot='angle-slider-track-rail']:stroke-green-100" />
```

### Range Theming

```tsx title="angle-slider-snippet.tsx"
<AngleSliderRange className="stroke-green-500" />
```

### Thumb Theming

```tsx title="angle-slider-snippet.tsx"
<AngleSliderThumb className="border-green-500 bg-green-50 ring-green-500/50" />
```

### Value Theming

```tsx title="angle-slider-snippet.tsx"
<AngleSliderValue className="text-green-600 dark:text-green-400" />
```

## API Reference

### AngleSlider

The main container component for the angle slider.

<TypeTable
  type={{
    value: {
      description: 'Controlled value of the slider (array of numbers)',
      type: 'number[]',
    },
    defaultValue: {
      description: 'Default value for uncontrolled slider',
      type: 'number[]',
      default: '[0]',
    },
    onValueChange: {
      description: 'Callback when the value changes during dragging',
      type: '(value: number[]) => void',
    },
    onValueCommit: {
      description: 'Callback when the value is committed (drag ends)',
      type: '(value: number[]) => void',
    },
    min: {
      description: 'Minimum value of the slider',
      type: 'number',
      default: '0',
    },
    max: {
      description: 'Maximum value of the slider',
      type: 'number',
      default: '100',
    },
    step: {
      description: 'Step increment for value changes',
      type: 'number',
      default: '1',
    },
    minStepsBetweenThumbs: {
      description: 'Minimum steps required between multiple thumbs',
      type: 'number',
      default: '0',
    },
    size: {
      description: 'Radius of the circular track in pixels',
      type: 'number',
      default: '60',
    },
    thickness: {
      description: 'Thickness of the track stroke in pixels',
      type: 'number',
      default: '8',
    },
    startAngle: {
      description: 'Starting angle of the track in degrees',
      type: 'number',
      default: '-90',
    },
    endAngle: {
      description: 'Ending angle of the track in degrees',
      type: 'number',
      default: '270',
    },
    dir: {
      description: 'Text direction',
      type: '"ltr" | "rtl"',
      default: '"ltr"',
    },
    name: {
      description: 'Name attribute for form submission',
      type: 'string',
    },
    form: {
      description: 'Form id to associate with',
      type: 'string',
    },
    disabled: {
      description: 'Whether the slider is disabled',
      type: 'boolean',
      default: 'false',
    },
    inverted: {
      description: 'Whether to invert the value direction',
      type: 'boolean',
      default: 'false',
    },
    asChild: {
      description: 'Merge props onto child element',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the angle slider is disabled.",
    },
    {
      title: "[data-slot='angle-slider']",
      value: "Present on the root element.",
    },
  ]}
/>

### AngleSliderTrack

The circular track that represents the full range of possible values.

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the angle slider is disabled.",
    },
    {
      title: "[data-slot='angle-slider-track']",
      value: "Present on the track element.",
    },
    {
      title: "[data-slot='angle-slider-track-rail']",
      value: "Present on the rail of the track.",
    },
  ]}
/>

### AngleSliderRange

The portion of the track that represents the selected range.

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the angle slider is disabled.",
    },
    {
      title: "[data-slot='angle-slider-range']",
      value: "Present on the range element.",
    },
  ]}
/>

### AngleSliderThumb

The draggable handle for selecting values.

<TypeTable
  type={{
    index: {
      description: 'Index of the thumb for multi-value sliders',
      type: 'number',
      default: '0',
    },
    asChild: {
      description: 'Merge props onto child element',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the angle slider is disabled.",
    },
    {
      title: "[data-slot='angle-slider-thumb']",
      value: "Present on the thumb element.",
    },
  ]}
/>

### AngleSliderValue

Displays the current value(s) with customizable formatting.

<TypeTable
  type={{
    unit: {
      description: 'Unit to display after the value',
      type: 'string',
      default: '"°"',
    },
    formatValue: {
      description: 'Custom formatter function for the value',
      type: '(value: number | number[]) => string',
    },
    asChild: {
      description: 'Merge props onto child element',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the angle slider is disabled.",
    },
    {
      title: "[data-slot='angle-slider-value']",
      value: "Present on the value element.",
    },
  ]}
/>

## Accessibility

The angle slider component includes comprehensive accessibility features:

### Keyboard Interactions

<KeyboardShortcutsTable
  data={[
    {
      keys: ["ArrowUp", "ArrowRight"],
      description: "Increase the value by one step.",
    },
    {
      keys: ["ArrowDown", "ArrowLeft"],
      description: "Decrease the value by one step.",
    },
    {
      keys: ["PageUp"],
      description: "Increase the value by ten steps.",
    },
    {
      keys: ["PageDown"],
      description: "Decrease the value by ten steps.",
    },
    {
      keys: ["Shift + Arrow Keys"],
      description: "Increase or decrease the value by ten steps.",
    },
    {
      keys: ["Home"],
      description: "Set the value to the minimum.",
    },
    {
      keys: ["End"],
      description: "Set the value to the maximum.",
    },
  ]}
/>

### ARIA Attributes

- Each thumb has `role="slider"` with `aria-valuemin`, `aria-valuemax`, and `aria-valuenow`
- The track has `aria-hidden="true"` as it's decorative
- Disabled state is communicated via `aria-disabled` and `data-disabled`

## Features

- Optimized for touch interactions on mobile devices
- Smooth dragging experience with proper pointer handling
- Full right-to-left language support
- Comprehensive keyboard navigation and screen reader support
- Angle ranges with minimum step constraints with multiple thumbs
- Controlled and uncontrolled state management
