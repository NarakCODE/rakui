## Qwen Added Memories
- When documenting component props in MDX files, use the `<TypeTable>` component instead of `<AutoTypeTable>` or `<PropsTable>`. The project uses `<TypeTable type={{ propName: { description, type, default } }}>` pattern for props documentation.
